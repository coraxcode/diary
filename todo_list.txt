Future Improvements

1 -    Compress a single TXT file using ZSTD, with SHA-512 verification (parameter: compress).

2 -    Compress the "Diary" directory into a TAR archive, applying ZSTD compression and SHA-512 verification (parameter: compress).

3 -    Implement decompression functionality for files or directories, with the program verifying SHA-512 before extracting (parameter: decompress).
